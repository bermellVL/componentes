import React from 'react';
import {
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  View,
} from 'react-native';
import { preset } from '../../../jest.config';

const Partit= ()=> {
    return(
        <view>
            <text>Levante cf - Valencia cf</text>
            <text> 5 - 0 </text>
        </view>
    );
};

export default Partit;